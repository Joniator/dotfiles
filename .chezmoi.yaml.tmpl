cd:
  command: nu

{{ $osId := (index .chezmoi.osRelease "id" | default "unknown") }}

data:
  isWindows: {{ contains "win" .chezmoi.os }}
  isWork: {{ contains .chezmoi.hostname "msgn" }}
  isCachy: {{ eq "cachyos" $osId }}
